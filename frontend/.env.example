# zorder web
# Vite loads env from the repo root (`../.env`) so deployment config stays centralized.
# Leave this unset when frontend and backend are served from the same origin.
# If needed, set this in the repo-root `.env`.
# VITE_API_BASE_URL=http://localhost:4000
