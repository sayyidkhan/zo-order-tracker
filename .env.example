# zorder app env
# Copy this file to `.env` in the repo root on Zo or locally.
# Do not commit `.env`.

# Zo injects PORT for the public service. Leave PORT unset for one-push deployments.
# PORT=8080

# Internal backend port used by the root deploy server.
ZORDER_BACKEND_PORT=4000

# SQLite persistence. Relative paths are resolved from `backend/`.
DATABASE_URL=file:./data/zorder.sqlite

# Simple MVP username + 6-digit PIN auth.
# Change these before any public demo.
ZORDER_USER_USERNAME=user
ZORDER_USER_PIN=123456
ZORDER_USER_EMAIL=user@example.com
ZORDER_ADMIN_USERNAME=admin
ZORDER_ADMIN_PIN=654321

# Workflow generation defaults to local deterministic templates.
# Set WORKFLOW_BUILDER_MODE=openai and OPENAI_API_KEY to enable setup-only OpenAI drafting.
WORKFLOW_BUILDER_MODE=local
# OPENAI_API_KEY=
# OPENAI_RESPONSES_URL=https://api.openai.com/v1/responses
# GPT_MODEL=gpt-5.5

# Optional. Leave unset when frontend and backend are served from the same origin.
# VITE_API_BASE_URL=http://localhost:4000
